- [x] `/settings/import` 页面中的“下载模板”入口已接成真实可点击下载，而不是占位说明。

- [x] 点击“下载模板”后，浏览器会下载一个 `.csv` 文件，文件名清晰可辨识。

- [x] 下载后的模板至少包含 `date`、`visibility`、`citation_rate` 字段，且列头顺序稳定。

- [x] 模板内容与页面样例表头、当前本地校验逻辑保持一致。

- [x] 若模板包含示例行，页面已清楚提示示例数据仅供参考、导入前可删除。

- [x] 下载过程未发起真实网络请求，也未依赖 Supabase Storage 或外部文件服务。

- [x] 页面仍保持纯白极简杂志排版，未因新增下载能力破坏现有层级和节奏。

- [x] 实现后未引入新的 lint、TypeScript 或诊断报错。

- 验证备注：`npm run lint` 通过；`src/app/settings/import/page.tsx` 与本次 spec 文档诊断为空。项目内若存在其他历史构建问题，未由本次模板下载功能引入。
