* [x] `/settings/import` 页面提供真实的 CSV 模板下载按钮，下载内容包含标准表头。

* [x] 页面可在前端成功解析本地 `.csv` 文件，并识别必填字段。

* [x] 表头存在前后空格时仍可被正确识别，缺失必填字段时会明确提示。

* [x] `date` 字段可被标准化为 `YYYY-MM-DD`；非法日期行会被跳过并记录原因。

* [x] 非法数字字段不会导致整体崩溃，系统会跳过错误行并展示成功/跳过摘要。

* [x] 合法记录会写入 Supabase `geo_metrics_daily`，并按 `brand_id + date` 执行 upsert。

* [x] 上传重复日期数据时会执行覆盖更新，而不是插入重复记录。

* [x] 页面对导入中、成功、部分跳过、失败四种状态都有清晰反馈，且不会白屏。

* [x] 页面视觉仍符合纯白极简杂志排版，没有因为真实导入能力而变得嘈杂。

* [x] 实现后未引入新的 lint、TypeScript 或构建错误。
