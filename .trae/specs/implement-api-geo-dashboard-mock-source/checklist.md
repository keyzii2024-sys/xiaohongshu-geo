* [x] 已新增 `/api/geo` 统一数据接口。

* [x] `Dashboard` 主内容区改为通过 `/api/geo` 获取数据，而不是直接读取页面内 mock 对象。

* [x] `/api/geo` 返回的字段覆盖品牌信息、时间范围、KPI、趋势图、告警、Top 问答与建议卡片所需内容。

* [x] 当前接口返回仍基于本地静态 mock 数据，未接入真实外部 API。

* [x] 页面在加载中、请求失败、请求成功三种状态下均有清晰且克制的表现，不会白屏。

* [x] 改造后页面视觉仍符合纯白极简杂志排版，不因数据获取方式变化而破坏版式。

* [x] 实现后未引入新的 lint 或 TypeScript 报错，`npm run build` 可通过。
