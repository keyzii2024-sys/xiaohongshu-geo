- [x] `/settings/import` 页面支持读取并解析本地 `.csv` 文件。

- [x] 当文件缺少必填字段时，页面会明确提示缺少哪些字段。

- [x] 当文件包含部分错误行时，页面会展示成功条数、跳过条数与主要错误原因。

- [x] 页面会展示解析后的局部导入预览，而不是只显示文件名。

- [x] 页面会明确提示当前仅为本地解析预览，尚未写入数据库。

- [x] 页面对空文件、非 CSV 文件和无数据行等异常情况有清晰反馈。

- [x] 页面仍保持纯白极简杂志排版，无复杂装饰和误导性成功状态。

- [x] 解析流程未发起真实网络请求，也未写入 Supabase。

- [x] 实现完成后未引入新的 lint、类型错误或诊断报错。
